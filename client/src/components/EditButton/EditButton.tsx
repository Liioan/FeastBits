//. styles
import styles from './EditButton.module.css';

export default function EditButton() {
  return (
    <button className={`material-symbols-outlined ${styles.btn}`}>
      border_color
    </button>
  );
}
